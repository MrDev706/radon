const batch = "    Function Up Radon cohort   ";

const trimS = batch + batch.trim();

const upperCase = batch.toUpperCase();
const lowerCase = batch.toLocaleLowerCase();
module.exports  = {
     trimS: trimS,
     changeToUp: upperCase,
     changeToLw: lowerCase


}


